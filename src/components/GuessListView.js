import React from 'react'

//need function to grab the guessed user number and attach it to ul

// guess list container
const GuessList = ({}) => (
  //var guesses =  map over the guessses in our state or props  (array of guess)
    // <li>num</li>
  <div>
    <ul id="guessList" className="guessBox clearfix">
      // guesses
    </ul>
  </div>
)

export default GuessList
